<!DOCTYPE html>
<html lang="en">
<%= render "shared/head" %>
<body>
  <div class="navbar navbar-fixed-top">
    <div class="navbar-inner">
      <div class="container">
        <%= link_to (Appetizer.appname+' <span class="betasig">Beta</span>').html_safe, root_path, :class => 'brand global-bigger' %>
        <div class="nav">
          <li class="divider-vertical"></li>
        </div>
        <%= render "shared/not_logged_menu" %>
        <%= render "shared/user_menu" %>
        <%= render "shared/search_bar" %>
      </div>
    </div>
  </div>