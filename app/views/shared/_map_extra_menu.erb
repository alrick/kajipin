<ul class="nav">
  <li>
    <%= link_to '<i class="icon-globe"></i> Fit World'.html_safe %>
  </li>
  <li class="dropdown">
    <a href="#" class="dropdown-toggle" data-toggle="dropdown">
      <i class="icon-list"></i> Fit Countries
      <b class="caret"></b>
    </a>
    <ul class="dropdown-menu dropdown-menu-countries">
      <% @countries.each do |country| %>
        <li>
          <%= link_to country["name"], params.replace(:w => country["west"], :e => country["east"], :n => country["north"], :s => country["south"]) %>
        </li>
      <% end %>
    </ul>
  </li>
</ul>