<h1>Listing logbooks</h1>

<table>
  <tr>
    <th>Description</th>
    <th>Location</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @logbooks.each do |logbook| %>
  <tr>
    <td><%= logbook.description %></td>
    <td><%= logbook.location_id %></td>
    <td><%= link_to 'Show', logbook %></td>
    <td><%= link_to 'Edit', edit_logbook_path(logbook) %></td>
    <td><%= link_to 'Destroy', logbook, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Logbook', new_logbook_path %>
