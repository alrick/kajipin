<% if can? :read, @user %>
  <a id="map-countries" class="map-nav map-zoom" rel="tooltip" title="Fit Country" data-placement="right">
    <i class="icon-list-alt"></i>
  </a>
  <ul id="map-countrieslist">
    <% @countries.each do |country| %>
      <li class="map-countryitem" data-north="<%= country["north"]%>" data-east="<%= country["east"]%>" data-south="<%= country["south"]%>" data-west="<%= country["west"]%>">
        <%= country["name"] %>
      </li>
    <% end %>
  </ul>
<% end %>
<a id="map-world" class="map-nav map-zoom" rel="tooltip" title="Fit World" data-placement="right">
  <i class="icon-globe"></i>
</a>