<% content_for :page_title do %><%= @user.first_name %>'s Pins<% end %>

<!-- Include additionals map controls -->
<%= render "users/controls" %>

<!-- Include side pins -->
<% if !@pins.nil? && !@pins.empty? %>
	<% content_for :side do %>
		<%= render "pins/side-pins" %>
	<% end %>
<% end %>